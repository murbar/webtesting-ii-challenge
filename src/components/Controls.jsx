import React from 'react';
import styled from 'styled-components';

const Styles = styled.div``;

const Controls = ({ gameData }) => {
  return <Styles>Controls</Styles>;
};

export default Controls;
